server {
    listen 80;
    
    root /usr/share/nginx/html;
    
    location / {
        # Cấu hình rewrite cho Admin SPA
        try_files $uri $uri/ /index.html;
    }

    # ❌ XÓA HOẶC COMMENT KHỐI location /api NÀY
    # location /api {
    #     proxy_pass http://backend:10000; 
    #     proxy_set_header Host $host;
    #     # ... (các dòng proxy khác) ...
    # }
}